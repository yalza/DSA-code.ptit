#include<iostream>
#include<vector>
using namespace std;
int main() {
	int n, a; cin >> n;
	long long sum = 0;
	for (int i = 0; i < n; i++) {
		cin >> a;
		if (a > 0)sum += a * 2;
	}
	cout << sum << endl;
}
