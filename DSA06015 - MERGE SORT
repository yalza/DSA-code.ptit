#include<iostream>
#include<algorithm>
using namespace std;
int main() {
	int t; cin >> t;
	while (t--) {
		int n; cin >> n;
		int M[1000000];
		for (int i = 0; i < n; i++)cin >> M[i];
		sort(M, M + n);
		for (int i = 0; i < n; i++)cout << M[i] << " ";
		cout << endl;
	}
}
