#include<iostream>
using namespace std;
int main() {
	int t; cin >> t;
	while (t--) {
		long long n, x, a;
		cin >> n >> x;
		for (long long i = 0; i < n; i++) {
			cin >> a;
			if (a == x)cout << i + 1 << endl;
		}
	}
}
